package gallery.app.common

import androidx.recyclerview.widget.RecyclerView
import androidx.viewbinding.ViewBinding


/*abstract class GenericAdapter<T>(val itemList:List<T>
//,val itemCallback: DiffUtil.ItemCallback<T>
) :
    RecyclerView.Adapter< BaseViewHolder<out ViewBinding, T>>() {

    override fun onBindViewHolder(holder: BaseViewHolder<out ViewBinding, T>, position: Int) {
        holder.bindTo(itemList.get(position))
    }

    override fun getItemCount(): Int = itemList.size
}*/
